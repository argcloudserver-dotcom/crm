{
  "buildCommand": "pnpm --filter @workspace/web build && pnpm --filter @workspace/api build:serverless && echo 'Copy static files' && cp -r apps/web/dist/public/* .",
  "installCommand": "pnpm install --no-frozen-lockfile",
  "routes": [
    { "src": "/api/(.*)", "dest": "/api" },
    { "src": "/(.*)", "dest": "/index.html", "status": 200 }
  ]
}
